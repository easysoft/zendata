fields:
  - field: first
    range: 14_config.yaml{6}                                # 注意此处{10}为取10条记录，非重复语法。
    prefix: "From 16.yaml\t"
    postfix: "\t|\t"

  - field: second
    range: 15_from_range.yaml{3},16_from_instance.yaml{6}    # 注意此处{5}为取5条记录，非重复语法。
    prefix: "From 17.yaml: \t"
